# Configurations

Future experiment configurations live here. A configuration must identify the model adapter, checkpoint, active shared/private blocks, task data manifests, optimizer schedules, compensation estimator, negotiation solver, evaluation suite, and output root through portable fields or environment variables.

Machine-local paths and credentials never belong in committed configuration files.

