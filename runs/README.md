# Run metadata

Each formal run uses `runs/<run_id>/` with a sanitized resolved configuration and a factual note. Logs, checkpoints, generated media, and other large artifacts are ignored and referenced by portable external identifiers.

