# Future package boundary

The future `comppareto` package is planned to contain four independent interfaces:

1. model adapters exposing shared and private parameter blocks;
2. private-response estimators (unrolled, implicit, diagonal/low-rank);
3. retained-gain/Pareto negotiation solvers;
4. diagnostic and certificate logging.

No implementation is added before the T0 review and implementation plan are accepted.

