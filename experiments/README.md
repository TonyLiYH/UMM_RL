# Experiments

This directory will contain executable experiment manifests and launch documentation. Research rationale belongs in `docs/plans/`; factual outcomes belong in `PROGRESS.md` and sanitized `runs/<run_id>/` metadata.

Experiment implementation starts only after the corresponding decision gate in `PROGRESS.md` has a frozen pass condition.

